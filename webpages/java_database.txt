 1: 
	CREATE TABLE REGISTERUSER
     (	
    "USERID" NUMBER NOT NULL ENABLE, 
	"FIRSTNAME" VARCHAR2(50), 
	"LASTNAME" VARCHAR2(50) NOT NULL ENABLE, 
	"AGE" NUMBER, 
	"ADDRESS" VARCHAR2(200), 
	"USERNAME" VARCHAR2(50) NOT NULL ENABLE, 
	"PASSWORD" VARCHAR2(50) NOT NULL ENABLE, 
	"ROLE" NUMBER, 
	"EMAILID" VARCHAR2(200) NOT NULL ENABLE, 
	 CONSTRAINT "REGISTERUSER_PK" PRIMARY KEY ("USERID")
     );
	 
	 CREATE SEQUENCE userid_seq START WITH 1 INCREMENT BY 1 MINVALUE 1 MAXVALUE 100000;

2.	 
	CREATE TABLE "SYSTEM"."ROLE" 
	(	
	"ROLEID" NUMBER NOT NULL ENABLE, 
	"ROLE" VARCHAR2(100 BYTE) NOT NULL ENABLE
	)
	
3.	 
  CREATE TABLE "SYSTEM"."JOBS" 
   (	
    "JOBID" NUMBER NOT NULL ENABLE, 
	"TITLE" VARCHAR2(100 BYTE) NOT NULL ENABLE, 
	"DESCRIPTION" VARCHAR2(200 BYTE), 
	"JOBTYPE" NUMBER NOT NULL ENABLE, 
	"STATUS" NUMBER, 
	"EXPIRATION_DATE" DATE NOT NULL ENABLE
   ) 
 CREATE SEQUENCE jobid_seq START WITH 1 INCREMENT BY 1 MINVALUE 1 MAXVALUE 100000;
 
 4.
	 job types
	enum{ fulltime,	 parttime}
	
	CREATE TABLE "SYSTEM"."JOBTYPES" 
   (	
    "JOBTYPEID" NUMBER NOT NULL ENABLE, 
	"JOBTYPE_NAME" VARCHAR2(100 BYTE) NOT NULL ENABLE, 
	"STATUS" NUMBER NOT NULL ENABLE
   )
	 
5. CREATE TABLE "SYSTEM"."JOB_APPLICATIONS" 
   (	
    "JOBID" NUMBER, 
	"EMPID" NUMBER, 
	"STATUS" NUMBER DEFAULT 1, 
	"CREATED_ON" DATE, 
	"UPDATED_ON" DATE
   )
   
     part time timing
	 
	 job applications files
	 
	 
	 expiration date